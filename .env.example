# Copie para .env.local (ignorado pelo git) e preencha com o valor real.
# Usado só pelo remendo temporário de acesso (ver src/components/PasswordGate.jsx)
# — some quando a migração para autenticação Supabase acontecer.
VITE_APP_PASSWORD=
